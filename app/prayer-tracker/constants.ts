export const ARCHIVED_CATEGORY = "Archived";

/**
 * Default prayer assignment capacity for adult members
 */
export const DEFAULT_ADULT_CAPACITY = 2;

/**
 * Default prayer assignment capacity for child members
 */
export const DEFAULT_CHILD_CAPACITY = 1;

/**
 * Minimum number of families to fetch for rotation, even if total capacity is lower
 */
export const MIN_FAMILY_LIMIT = 2;

/**
 * Maximum number of personal prayer requests to show in a single rotation
 */
export const MAX_PERSONAL_REQUESTS_PER_ROTATION = 5;
